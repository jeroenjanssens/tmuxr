dist: bionic
language: r
r: release

r_packages:
  - covr

apt_packages:
  - tmux

warnings_are_errors: true

notifications:
  email:
    on_success: change
    on_failure: change

after_success:
  - Rscript -e 'library(covr); codecov()'
